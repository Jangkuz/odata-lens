import { ODataUrl } from '../ast'

/**
 * Print AST in compact canonical form (single-line URL).
 */
export function printCompact(ast: ODataUrl): string {
  // Stub for Phase 1. Real implementation in Phase 3.
  return ''
}

/**
 * Print AST in expanded form (multi-line, indented, readable).
 */
export function printExpanded(ast: ODataUrl): string {
  // Stub for Phase 1. Real implementation in Phase 3.
  return ''
}
